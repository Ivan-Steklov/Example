CREATE TABLE  Animals (
                         id SERIAL PRIMARY KEY,
                         name VARCHAR(255) NOT NULL,
                         age INT NOT NULL,
                         type VARCHAR(50)

);